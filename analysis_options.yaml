include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
  errors:
    experimental_member_use: ignore

linter:
  rules:
    # avoid_print: false
